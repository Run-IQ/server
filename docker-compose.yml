services:
  run-iq-server:
    build:
      context: .
      target: full
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      HOST: 0.0.0.0
      LOG_LEVEL: info
      STRICT: "true"
      PLUGINS: fiscal
      DSLS: jsonlogic
